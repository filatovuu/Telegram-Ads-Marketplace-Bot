# JWT
APP_JWT_SECRET=change-me-in-production
APP_DEBUG=true

# TON escrow
APP_TON_PLATFORM_MNEMONIC=word1 word2 word3 ... word24
APP_DEAL_EXPIRE_HOURS=72
APP_DEAL_REFUND_HOURS=48
APP_PLATFORM_FEE_PERCENT=10

# MTProto (optional — for enhanced channel analytics)
# Get api_id and api_hash from https://my.telegram.org
# Generate session string: cd backend && python -m scripts.generate_session
MTPROTO_API_ID=
MTPROTO_API_HASH=
MTPROTO_SESSION_STRING=
